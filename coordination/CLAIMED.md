# File Claims — Father Eye

> **PROTOCOL: Before editing ANY file, claim it here, commit, and push.**
> **Before starting work, `git pull` and check nobody else has claimed your target files.**
> **Release claims (delete the row) when you're done and have pushed your changes.**

> **Remote:** `https://github.com/hypnofrenzy76/father-eye.git` (origin/main).

## How It Works
1. `git pull` to get latest claims
2. Check if the files you need are already claimed below
3. If NOT claimed, add your claim row, commit + push
4. Do your work
5. When done, remove your claim row, commit your code changes + the updated CLAIMED.md

## Task-ID scheme
<!-- Category counters (next ID): Bld-12, Mc-03, Brg-28, Pnl-76, Map-02, Doc-05, Web-04, Bkp-02 -->
<!-- Pnl-75 completed 2026-06-14: panel Stop button regression fixed. User: "also server stop button is no longer responding". Root cause: Bkp-01 rewrote the pre-stop backup from a fast local uncompressed world copy into a compressed tar|gzip stream of the whole world to the external "Server Backups" volume, fronted by an RCON save-off/flush/save-on bracket. On a large world that runs for MINUTES, and App.java's stop handler ran BackupService.fromConfig(appConfig).runBackup() INLINE on its worker thread with NO UI feedback before calling launcher.stop(), so the Stop button looked dead: badge stayed RUNNING, server kept running the whole time, and a slow/unmounted external drive could strand the stop indefinitely. Fix (panel/.../App.java only): (1) immediate update("Pre-stop backup running, then stopping...") the instant Stop is clicked so the operator sees action; (2) new PRE_STOP_BACKUP_TIMEOUT_MS=90_000 ceiling + runPreStopBackupBounded() runs the backup on a dedicated daemon FatherEye-PreStopBackup ExecutorService and Future.get(timeout) waits at most 90s; on TimeoutException it logs + status-lines "backup continues in background" and proceeds to stop (worker.shutdown(), NOT shutdownNow(), so the overrunning backup finishes its own RCON save-on / archive cleanup rather than leaving a half-written archive or saving disabled); ExecutionException/InterruptedException/RejectedExecutionException(+unchecked fromConfig path errors) all caught so the helper NEVER propagates; (3) the stop handler wraps the bounded backup in try/finally so launcher.stop() is ALWAYS reached. Triple audit (3 parallel Opus agents) fixes adopted: DATA SAFETY (MC-78635) - if the bounded wait times out the backup's save-off bracket may still be open (autosave disabled) when /stop is sent, and a server stopped with autosave off can skip its final chunk flush and lose recent changes; defensively send launcher.sendCommand("save-on") on the live server's own stdin in the finally BEFORE launcher.stop() (best effort; if stdin is gone the server is already down); plus the RuntimeException guard around submit()/fromConfig() noted above so a scheduler/path failure can never skip the stop. Build green: ./gradlew :bridge:compileJava :webportal:compileJava :panel:compileJava :webportal:test all UP-TO-DATE/passing. No version bump (panel-only behavior fix on 0.3.3-mac.1). Pre-existing em-dash style nits in App.java (lines 39/151/295/810/861/1140, all in code untouched by this change) left for the user to decide on rather than churning unrelated lines. Files: panel/src/main/java/io/fathereye/panel/App.java, CHANGELOG.md, memory/MEMORY.md + new memory entry. -->
<!-- Bkp-01 + Web-03 + Pnl-74 + Brg-27 completed 2026-06-14: compressed external backups + NEW live player-state restore. Problem: old BackupService wrote full UNCOMPRESSED world+config into /Users/luke/Desktop/Server/backups (18 GB on the internal disk); no independent world/playerdata rollback; no way to restore a single player's inventory/full state while the server runs. Solution part 1 (Bkp-01/Web-03/Pnl-74): backups now go to the external "/Volumes/Server Backups/backups" volume as gzip archives in a structured per-run dir fe-<timestamp>/{world.tar.gz (world minus player-owned subtrees), playerdata.tar.gz (only player-owned subtrees), optional server-config archive, JSON manifest}; fe-rollback.sh restores either stream atomically and INDEPENDENTLY; space-based retention prunes oldest fe-* (and legacy thaumaturgy-*.tar.gz) above the free-GB floor; web portal BackupManager + /api/backup/* routes drive a Backups tab (list/run/independent rollback). Solution part 2 (Brg-27, NEW live player restore via NBT injection): PlayerRestoreService (webportal) extracts exactly one <uuid>.dat from a backup's playerdata.tar.gz via tar (-tzf to list, -xzO to extract one member), inputs tightly validated (backup id fe-YYYYMMDD-HHMMSS, player id canonical dashed UUID) so nothing can smuggle a path/option into the tar arg list (no shell, literal args); base64 -> bridge op player_restoreState (WebPortalMain routes /api/backup/players GET + /api/player/restore POST, 30s timeout, requires bridge connected else 409). PlayerRestoreHandler (bridge, registered in RpcHandlers, runs on the TICK thread so live entity touch is safe): decodes NBT, takes a timestamped pre-restore safety snapshot under playerdata/fe-pre-restore/ (live entity serialised for online, on-disk file copied for offline) so a bad restore is undoable; ONLINE -> ServerPlayerEntity.load(nbt) (same entry point vanilla login uses) then resends inventory (SWindowItemsPacket), health (SUpdateHealthPacket), XP (SSetExperiencePacket), abilities (SPlayerAbilitiesPacket) + teleport so the client screen updates with no disconnect; OFFLINE -> atomic write (*.fe-tmp + ATOMIC_MOVE) over playerdata/<uuid>.dat for next login. UI: a "Live Players" action on each backup row opens a modal listing players in that backup; click restores. Triple audit (3 parallel Opus reviewers: Minecraft-correctness, security, JS/UI) + fixes applied: (1) CROSS-DIMENSION DESYNC (critical) - Entity.load() sets the dimension reference but does NOT move the entity between source/target ServerWorld registries; the follow-up teleportTo would try to remove it from a world it was never registered in (ghost/desync). Fix: when the backup's target dim differs from the player's current dim, strip "Dimension" from a COPY of the NBT before load() (entity stays in its correctly-registered world) then do an explicit fully-registered cross-dimension teleportTo. (2) TAR PIPE DEADLOCK (critical) - listing merges stderr into stdout (redirectErrorStream(true), harmless for a text listing); single-member extraction keeps stderr SEPARATE (so tar diagnostics can't corrupt the binary .dat on stdout) and drains it on a dedicated daemon thread so a chatty tar can't fill the stderr pipe and deadlock the stdout reader. (3) MEMORY GUARD - MAX_DAT_BYTES=32 MB aborts an oversized extract (destroyForcibly) instead of unbounded buffering. (4) one JS-escaping flag verified a FALSE POSITIVE (\\'s in the Java text block emits valid JS \'s) and left as-is. Disk cleanup: verified the newest external full archive (thaumaturgy-20260613-224348.tar.gz, 16.6 GB) is a valid gzip/tar with the world before deleting the stale 18 GB internal folder world-20260613-222138-849 (+ .DS_Store); Data-volume free space 147 GiB -> 166 GiB (~19 GB reclaimed); internal backups/ dir now empty. Note: existing external archives use the legacy thaumaturgy-*.tar.gz single-archive naming; the structured fe-*/{world,playerdata}.tar.gz layout the live-restore feature reads is produced on the next backup run, legacy archives kept as a safety net and pruned by retention. Build: ./gradlew :bridge:compileJava :webportal:compileJava clean; ./gradlew build -x test green (embedded bridge bundle re-emitted into setup). PENDING: live online+offline restore test on a running server (next run also produces the first fe-* structured backup the restore UI consumes). No version bump (additive feature + ops fix on 0.3.3-mac.1). Files: bridge/.../rpc/PlayerRestoreHandler.java (new), bridge/.../rpc/RpcHandlers.java, webportal/.../PlayerRestoreService.java (new), WebPortalMain.java, WebPortalAssets.java, BackupManager.java (new), PortalConfig.java (new), scripts/fe-backup.sh, scripts/fe-rollback.sh, CHANGELOG.md, memory/MEMORY.md + new memory entry. -->
<!-- Web-01 completed 2026-06-14: new webportal module committed (was claimed 2026-06-13, work finished and pushed this session). Browser-based remote-control portal served by a JDK-only HTTP server over Tailscale; reuses the panel IPC layer (TCP-only, no JNA/JavaFX), PBKDF2 long-password auth with constant-time compare and lockout, RFC 6455 WebSocket for live topic streaming. 17 source files under webportal/src plus webportal/build.gradle and the settings.gradle include line. Built and tested clean. -->
<!-- Web-02 completed 2026-06-14: web portal dashboard tabs were all empty (Stats/Players/Mobs/Mods/Map) while the server ran; only Console populated. User: "nothing is appearing in any of tabs when i connect to the web portal while server is running". Root cause: the bridge wraps every Snapshot/Delta payload as { seq, data } (docs/ipc-protocol.md lines 58-60); the JavaFX panel unwraps payload.get("data") before reading topic fields (StatsPane/PlayersPane/MobsPane/ModsPane/MapPane) but webportal BridgeConnection.handleTopic cached (latestSnapshots) and fanned out the RAW wrapper, while the browser JS in WebPortalAssets.java reads topic fields directly (p.tps20s, p.players, byDim, ...) so every snapshot tab found nothing one level too deep and stayed blank. console_log is an Event topic (NOT wrapped) so Console worked, which was the diagnostic tell. Diagnosed live: bridges dir was empty only because the server was stopped at the time (bridge writes its TCP marker while running, deletes on shutdown); server log confirmed bridge binds 127.0.0.1:<port> and portal connects, so WS was healthy and the data SHAPE was the bug. Fix: handleTopic now unwraps data for kind Snapshot/Delta (guarded payload!=null && payload.has("data")) before latestSnapshots.put and listener fan-out; Event payloads pass through verbatim; single central unwrap keeps the live-push path and the browser-connect initial-state path (allLatest()) consistent. Field names verified to match post-unwrap (tps20s/heapUsedBytes/heapMaxBytes/processCpuLoad/liveThreadCount; players[]; mobs/chunks/mods byDim) and the JS reads no seq/data field. New end-to-end regression test BridgeConnectionUnwrapTest: mock TCP bridge + marker + real BridgeConnection connect/handshake/subscribe/reader path, asserts unwrapped delivery on both listener and cached paths and raw console_log; waits on a connected-latch first to avoid a connect-vs-topic ordering race; marker cleanup hardened with JVM shutdown hook + deleteOnExit since the bridges dir is shared with a real bridge; stable across repeated --rerun-tasks runs. Triple audit (3 parallel Opus agents): consensus PASS, no regressions, no remaining field mismatch. Two latent items flagged to user, NOT changed (rule 7, connection core logic): (1) ensureConnected dispatches onConnected AFTER subscribes so a topic frame can reach a listener before the welcome callback (benign: browser reads welcome()/allLatest() on attach); (2) Map tab still needs chunk_tile RPCs and Arcanum tab needs its capability, so those can render late for reasons unrelated to this fix. No version bump (portal-only behavior fix). Files: webportal/.../BridgeConnection.java, webportal/src/test/.../BridgeConnectionUnwrapTest.java (new), CHANGELOG.md, memory/MEMORY.md + new memory entry. -->
<!-- NOTE 2026-06-14: could not git pull --rebase before editing (working tree had pre-existing uncommitted Web-01/Pnl-73 changes from prior sessions; rebase refused). Did not stash or clobber those. Edits confined to the webportal/** module already claimed by Web-01 below. -->
<!-- Pnl-73 completed 2026-06-12: Arcanum Players tab fixed panel-side. User: "quitting panel should stop server, we already have an active player list, just use that and cache any joined players name". Decisions recorded: panel-quit-stops-server is intended (anti-orphan hook stays); player tab fix lives in the panel, not the bridge. Root cause: bridge arcanum_getPlayers returns a UUID-keyed map with homesCount (ArcanumBridge.java:144) while ArcanumPane.processPlayersResponse expected an array under "players" with field "homes", so the table never populated and the rank ChoiceBox never appeared. Fix: processPlayersResponse parses the UUID-keyed map (entries deep-copied before injecting the uuid key, array shape still accepted, homesCount with homes fallback); new ArcanumPane.onPlayersSnapshot consumes players_topic (wired in App.java:704 next to PlayersPane/MapPane) to track online UUIDs and refresh rows via TableView.refresh() on the FX thread; persistent uuid->name cache at PlatformPaths.appDataDir()/arcanum-player-names.json resolves offline "Unknown" names, saved async only on change. Triple audit (3 parallel agents): adopted deepCopy-before-mutation, previous-value compare on cache put, TableView.refresh() over list self-setAll, async save; rejected clear()+addAll for onlineUuids (empty-set window) and the alleged equals NPE (non-null receiver); accepted minors logged in CHANGELOG. Version kept 0.3.3-mac.1 (panel-only, bridge handshake pin unchanged). dist/Father Eye.app rebuilt and /Applications/Father Eye.app replaced; methods verified in packaged jar via javap; Setup app and server jars untouched. Note: counter said next Pnl-72 but Pnl-72 was already consumed by the ArcanumPane full-editor work (see class javadoc), so this task took Pnl-73 and the counter now reads Pnl-74. -->
<!-- Doc-04 completed 2026-06-12: stale doc version pins caught up, deferred item from Bld-11 closed. User first asked to delete map/docs/HANDOFF.md, was shown the map/** claim conflict plus the three live references (FatherEyeMap.java:11, mods.toml:14, map/build.gradle:2), and decided: keep HANDOFF.md, update the old version references instead. docs/mapcore-api-contract.md title/pin/MapCore-VERSION-row and map/docs/HANDOFF.md pinned-version line updated 0.3.1-mac.1 -> 0.3.3-mac.1 with dated notes; CONTRACT_MAJOR=0/CONTRACT_MINOR=2 unchanged (API surface identical). mapcore 0.3.3-mac.1 published to local-maven/ per the HANDOFF bump workflow so the pin resolves; 0.3.2-mac.1 intentionally never published (all subprojects use project(':mapcore')). mapcore tests pass. Triple audit (3 parallel agents, one re-dispatched after empty output): all PASS; metadata checksums verified, jar VERSION constant verified via javap, .gitignore !local-maven/** negation confirmed. map/docs/HANDOFF.md doc-only edit inside the parallel session's map/** claim explicitly authorized by maintainer. -->
<!-- Bld-11 completed 2026-06-12: blackscale evil eye app icon + unified version bump 0.3.2-mac.1 -> 0.3.3-mac.1. User: "i want a new icon for father eye, needs to look like the evil eye but blackscale" + "make sure to bump version for clarity sake and commit" + "replace the father eye app in my applications folder and update the server bridge if needed for version to check correctly". Icon: nazar amulet in dark monochrome, generated by panel/icons/gen_icon.py (pure-stdlib PNG writer, Pillow not installable on the build Mac) + gen_icon.sh (sips + iconutil) producing panel/icons/fathereye.icns; wired via jpackage --icon into :panel:jpackageMacApp, :panel:jpackageMacDmg, :setup:jpackageMacApp. Versions: gradle.properties (mapcore/bridge/panel/map), App.VERSION, Constants.BRIDGE_VERSION, PlayerHeadCache User-Agent, MapCore.VERSION, MapCoreTest assertion; bridgeApiVersion untouched (0.1.0). Triple audit (3 parallel agents): consensus fix was stale 0.3.2-mac.1 fallback literals in setup/build.gradle:7,44 plus comment examples in panel/build.gradle:162,292, all updated; docs/mapcore-api-contract.md and map/docs/HANDOFF.md still pin 0.3.1-mac.1 against the published local-maven artifact and the other session's map/** claim, deferred to maintainer. Built bridge shadowJar + both jpackage apps; verified icns md5 identical in both bundles, CFBundleVersion 1.3.3, jars carry 0.3.3-mac.1. Deployed Father Eye.app + Father Eye Setup.app to /Applications and fathereye-bridge-0.3.3-mac.1.jar to /Users/luke/Desktop/Server/mods/ (0.3.2 removed) so the panel/bridge handshake version check matches. -->
<!-- Brg-26 + Pnl-71 completed 2026-06-12: true MSPT + map reaches 100% + cmd_run slash strip, version 0.3.2-mac.1. User: "mspt load is still 50 with no players" + "map is never reaching" + "its saying 789 unrenderable". Spark profile fCQsoH1Y4v (300s idle) showed server thread 91.9% parked with ~1.7ms real work while panel read "50ms MSPT". Brg-26 TpsCollector: rolling MSPT window stored tick-to-tick interval (floors at ~50ms because the scheduler sleeps out each 50ms slot); now stores Phase.START->Phase.END work duration, same quantity lastTickMs() already exposed. Brg-26 TickStateMirror: chunks whose render returns null (LOAD-level holders, no usable surface) reported in chunks_topic as version -1 (only when cache version == 0 — an unrenderable chunk with an older cached tile reports the REAL version so fresh sessions still fetch the cached tile); requestRender skips currently-unrenderable chunks; before this the panel's 5s rejected-retry loop re-requested all 789 such chunks forever, each retry re-rendering null on the tick thread and saturating the 1.5ms/tick budget (3% server-thread cost in profile, missEnqueued=7990/60s). 30s refresh rotation clears the set so promotions still render. Pnl-71 MapPane: version -1 counted as handled (rejectedChunks) with NO RPC so the bar hits 100% with "(N unrenderable)" suffix; new volatile unrenderableChunks snapshot set gates viewport fan-out; cache verification never samples unrenderable chunks and an all-zero sentinel never counts as world mismatch (prevents false runtime cache wipe). Brg-26 RpcHandlers.cmdRun strips leading slashes (Brigadier rejects "/spark ..." dispatched programmatically: "Unknown or incomplete command ... at position 0"). Triple audit: 2 of 3 raised thread-safety of unrenderable set + the unr&&v==0 condition; resolved — set is CHM.newKeySet (safe cross-thread), v>0 behaviour documented as deliberate in code comment. Versions: all artifacts 0.3.1-mac.1 -> 0.3.2-mac.1. Built shadowJar + jpackageMacApp, deployed bridge jar to /Users/luke/Desktop/Server/mods/ (old 0.3.1 removed) and Father Eye.app to Desktop. -->
<!-- Pnl-69 completed 2026-04-27: counter semantics fix, version 0.2.9. User: "still hangs at a percentage and doesnt reach 100" + "i need something that actually fixes this". v0.2.8 fixed the RPC leak but counter math still excluded in-flight chunks. Counter root cause: handled = tileImages + rejected excluded chunks currently in `requested` (in-flight RPCs). On a 2025-chunk world, after snapshot fired RPCs panel had ~600 in requested + ~1400 in tileImages. handled=1400, expected=2025, pct=69%. As chunks resolved percentage climbed but new chunks entering chunks_topic (player exploration, server saves) kept replenishing requested, bar never hit 100%. Pnl-69 fix: handled = tileImages + rejected + pending where pending = requested.size(). Once panel has acknowledged (sent RPC for) every reported chunk, bar shows 100%. New exploration causes brief dips when chunks_topic adds entries panel hasn't yet sent RPCs for; next 1Hz snapshot recovers within a second. Pending chunks eventually transition to tileImages (success) or rejectedChunks (timeout via Pnl-67 orTimeout / Pnl-68 sweep). Pnl-69 synchronous bookkeeping in all paths: dim-mismatch and all-zero/mostly-black paths in onChunkTileResponse were still wrapping requested.remove + rejectedChunks.put in Platform.runLater. Pnl-68 only fixed failChunkRequest. Now all three rejection paths plus success path do map mutations synchronously off FX thread; only label update / redraw stays in runLater. Versions: bridge 0.2.8 -> 0.2.9, panel 0.2.8 -> 0.2.9. Built and deployed. -->
<!-- Brg-23 + Pnl-68 completed 2026-04-27: stop the 594-stuck-RPCs leak, version 0.2.8. User: "40 mins of uptime and all chunks still have not loaded" + "use many agents and figure it out". Three parallel investigation agents identified three independent root causes combining to leak 594 chunk_tile RPCs forever. Brg-23 render-loop guard: ChunkTileHandler's inner 256-pixel loop only protected state.getMapColor with try/catch; chunk.getHeight, chunk.getBlockState, chunk.getBiomes could all throw on partially-loaded holder-fallback chunks (Brg-19 returns whatever IChunk is held, including ProtoChunks with null Heightmap or null ChunkSection[]). Inner loop now wraps the y/state lookup in try/catch with AIR fallback; getBiomes() also guarded. Brg-23 IpcSession swallow surfaced: Error response sendResponse sometimes failed (pipe back-pressure / closed connection), IOException silently swallowed at IpcSession.handleRequest's `catch (IOException ignored)`. Now logs at WARN with requestId. Pnl-68 requested -> ConcurrentHashMap<Long,Long> with timestamp: switched from LinkedHashSet<Long>; failChunkRequest now does requested.remove + rejectedChunks.put SYNCHRONOUSLY without Platform.runLater (only updateLoadingProgress goes through runLater). Earlier wrapper queued behind saturated FX thread and left 594 entries stranded for minutes despite 30s orTimeout firing. Pnl-68 periodic sweep backstop: daemon thread FatherEye-RequestSweep runs every 5s, scans requested for entries older than REQUEST_TIMEOUT_MS=35s, force-fails them via failChunkRequest. Pnl-68 cache-verify orTimeout: Pnl-66 verify RPCs were missing orTimeout, added. Pnl-68 canonical key: success path now uses originally-requested (cx,cz) instead of response-echoed rcx/rcz (defensive). Pnl-68 requestVisibleTiles parity: pan/zoom debounce loop also skips recently-rejected chunks, mirroring onChunksSnapshot behaviour. Versions: bridge 0.2.7 -> 0.2.8, panel 0.2.7 -> 0.2.8. Built and deployed. -->
<!-- Pnl-67 completed 2026-04-27: chunk RPC timeout + uptime display, version 0.2.7. User: "is dynamically loading new chunks as i walk around but its never reaching %100 chunks loaded" + "heartbeat is sitting at 0 once server is running" + "i need to know how long server has been running". Loading-reaches-100% root cause: chunk_tile RPCs that got lost in transit (silent bridge drop, pipe-buffer overflow, server tick stall) had no timeout, sat in panel's `requested` set forever, loading bar plateaued at 31-55%. Pnl-67 timeout + reject-on-fail: every chunk_tile RPC now has 30s orTimeout (Java 9+ CompletableFuture.orTimeout). On timeout future completes exceptionally; whenComplete sees error; failChunkRequest now (a) removes from requested AND (b) marks rejected with timestamp via rejectedChunks.put(key, now). Chunk counts toward "handled" so percentage can reach 100%; existing 5s TTL retry kicks in for next snapshot. Heartbeat clarification: "Heartbeat: 0s" was correct (seconds since last heartbeat, not "0 heartbeats received"); user wanted server uptime. Pnl-67 uptime label: new "Uptime: Xh Ym Zs" Label in MainWindow toolbar via setServerStartedAtEpochMs(long) + AnimationTimer @1Hz, anchored to marker.startedAtEpochMs. App.tryConnect calls setServerStartedAtEpochMs(m.startedAtEpochMs) after handshake. App.onBridgeDisconnect calls setServerStartedAtEpochMs(-1) to reset to "Uptime: --". Versions: bridge 0.2.6 -> 0.2.7, panel 0.2.6 -> 0.2.7. Built and deployed. -->
<!-- Pnl-65 + Pnl-66 completed 2026-04-27: content-based cache verification, version 0.2.6. User: "new chunks are loading fine but the old world chunks are still persisting" + "panel has to check that chunk map actually matches current world". Pnl-65 level.dat creationTime unconditional: Pnl-64's level.dat fallback only ran when .world-id stamp was missing. v0.2.4 wrote stamp matching user's current bridge UUID on first launch, so v0.2.5 saw valid-looking stamp and skipped fallback even though cache was from deleted world. Now level.dat check runs on every open regardless of stamp state, uses BasicFileAttributes.creationTime() (not lastModifiedTime which server touches on periodic save). Only trusts creationTime when it's strictly older than lastModifiedTime (the signal that FS actually distinguishes them; equality means FS doesn't track creation -> skip heuristic to avoid wiping on every relaunch). Pnl-66 content verification: on first chunks_topic snapshot per session, panel picks up to 5 chunks present in BOTH disk cache and live world (tileData.containsKey + coords), fires chunk_tile RPCs to fetch them fresh from bridge, bit-compares surfaceArgb arrays. If strict majority (3+ of 5) differ, cache is from previous world - wipes in-memory and disk caches at runtime via new TileDiskCache.wipeAll() and re-renders. Strongest signal: bridge produces deterministic ChunkTileHandler output for unmodified terrain so 5 samples are sufficient to distinguish same-world from different-world with high confidence. Player-modified chunks tolerated (single-chunk mismatches don't trigger wipe; threshold is m*2 > sampleCount). Implementation: new MapPane.cacheVerificationDone volatile boolean (one verification per session), MapPane.verifyCacheAgainstWorld(coords) called from onChunksSnapshot Platform.runLater after request loop, MapPane.wipeCacheRuntime() clears tileImages/tileData/requested/rejectedChunks and calls tileCache.wipeAll, new MapPane.arraysSimilar(int[], int[]) does bit-exact comparison. Versions: bridge 0.2.5 -> 0.2.6, panel 0.2.5 -> 0.2.6. Built and deployed. -->
<!-- Pnl-64 completed 2026-04-27: stable counter at 100% + retroactive cache wipe via level.dat, version 0.2.5. User: "map is still showing the map from the old world, it is not updating to reflect the new world folder" + "is still not getting to %100". Old-world bleed-through root cause: Pnl-63 UUID stamp only triggered wipe when stored stamp was non-null AND different from current UUID. On first launch with v0.2.4 after existing install, stamp file didn't exist yet, comparison short-circuited, cache wrote new UUID without wiping, old tiles survived. Mismatch detection only kicked in for SECOND world delete. Pnl-64 level.dat fallback: when stamp file absent, compare world/level.dat.lastModifiedTime against oldest tile's lastModifiedTime via new oldestTileMtime helper that walks Files.walk + filter ends-with(".tile"). If world is newer than every tile, tiles must be from previous deleted world -> wipe. Then write new UUID stamp. Existing users with valid worlds keep tiles (level.dat older than tiles); only newcomers/world-regenerators wipe. Counter oscillation root cause: Pnl-63 evicted entries from rejectedChunks once 5s TTL expired, counter contribution dropped on every retry tick. When bridge sentineled again, entry re-added, counter climbed back. User saw bar oscillate around 87% indefinitely. Pnl-64 counter stability: keep entries in rejectedChunks continuously; TTL controls retry eligibility (now-ts < REJECTED_RETRY_MS in snapshot loop) not membership. Successful renders explicitly remove via rejectedChunks.remove(key) on the success path so a chunk that was rejected then later rendered isn't double-counted (handled = tileImages + rejectedChunks). Versions: bridge 0.2.4 -> 0.2.5, panel 0.2.4 -> 0.2.5. Built and deployed. -->
<!-- Pnl-63 completed 2026-04-27: TTL retry for rejected chunks + world-id cache invalidation, version 0.2.4. User: "is loading chunks when i walk around but still not all chunks load and map is disjointed with empty areas" + "doesn't reset when i delete the world and generate a new one". Screenshot at v0.2.3 showed Father Eye stuck at 86% with 80 unrenderable chunks visible as black gaps inside the explored area. Disjoint-gaps root cause: Pnl-62's rejectedChunks Set was permanent for the session. When user walked away, chunk transitioned FULL -> LOAD between snapshot and RPC, bridge sentineled, panel marked rejected. When user walked back chunk re-promoted to FULL but permanent skip never retried, chunk stayed as permanent black gap. Pnl-63 TTL retry: switched rejectedChunks Set<Long> -> ConcurrentHashMap<Long,Long> (key -> rejection-timestamp). Entries TTL out after REJECTED_RETRY_MS=5000ms and chunk becomes eligible for retry. Counter semantics preserved (rejected entries still count toward "handled" while alive). 5s balances quick gap-fill on walk-back vs RPC budget on chronically unrenderable chunks. onChunksSnapshot adds rejectedChunks.values().removeIf(ts -> now-ts >= REJECTED_RETRY_MS) before the request loop. World-reset root cause: TileDiskCache keyed only on serverDir; deleting <serverDir>/world/ and regenerating the world inside the same server folder left the panel cache untouched, new world painted on top of old terrain. Pnl-63 world-id stamp: TileDiskCache.open now accepts bridge's instanceUuid (persisted at <serverDir>/world/serverconfig/fathereye-instance.uuid, regenerated worlds get fresh UUID). Cache writes UUID into <serverDir>/fathereye-tiles/.world-id on open, compares on every subsequent open. On mismatch: wipes every *.tile under base dir (stamp file preserved with new UUID). New helper wipeTileFiles uses Files.walk + filter ends-with(".tile"). App.tryConnect passes welcome.instanceUuid through to TileDiskCache.open. Versions: bridge 0.2.3 -> 0.2.4, panel 0.2.3 -> 0.2.4. Built and deployed. -->
<!-- Brg-22 + Pnl-62 completed 2026-04-27: loading bar always reaches 100% no more hangs, version 0.2.3. User: "map panel is stopping loading and does not continue loading no matter how long i wait" + "hangs around 40%" + "map has to slowly load until it gets to 100%, it can never hang". Root cause: chunks_topic reported every ChunkHolder regardless of ticket level including LOAD-level (44) chunks whose ChunkSection[] had been freed. Brg-21 sentinel-rejected those at the bridge correctly (they have no usable data). Panel removed sentinels from `requested`; next chunks_topic snapshot re-added them; bridge re-rejected; cycle repeated indefinitely. Net: loading bar sat at 40-60% forever even though every renderable chunk was drawn. Brg-22 chunks_topic filter: WorldStateCollector now filters holders to chunks where getChunk(FULL, false) is non-null. Same predicate ChunkTileHandler uses for FULL render path, so by construction every chunk reported is renderable. LOAD-level chunks excluded; invisible to panel until they promote back to FULL. Panel's TileDiskCache preserves previously-rendered tiles across LOAD downgrades. Cost: ~µs per holder per snapshot, ~4ms for 4000 holders. Pnl-62 rejectedChunks set: new ConcurrentHashMap.newKeySet() tracks chunks panel has sentinel-rejected this session. onChunksSnapshot loop skips chunks in this set. Auto-pruned to chunks still in chunks_topic via retainAll(reportedKeys) so a chunk that fully unloads stops counting; cleared on dim switch. Pnl-62 progress counter: updateLoadingProgress now counts rejected chunks toward "handled" so percentage hits 100% when every reported chunk has been answered, even if some answers were sentinels. Label format becomes "Loading X / Y chunks (Z%)" plus "(N unrenderable)" suffix if any rejections. Versions: bridge 0.2.2 -> 0.2.3, panel 0.2.2 -> 0.2.3. Built and deployed; on next launcher boot mods/fathereye-bridge-0.2.2.jar auto-deletes and 0.2.3.jar takes its place. -->
<!-- Brg-21 + Pnl-61 completed 2026-04-27: black-rectangle artefacts gone, version 0.2.2. User: "map was loading fast at first but has hung", screenshot showed panel stuck at 1336/2025 (66%) with black 16x16 rectangles around the rendered area. Root cause: Brg-19's holder-level fallback returned in-memory IChunk for chunks at LOAD ticket-level. Their ChunkHolder is still in chunkMap (so chunks_topic listed them) but ChunkSection[] was freed under memory pressure. Every getBlockState returned AIR -> MaterialColor.NONE (col == 0) -> surfaceArgb 0xFF000000. Pnl-56 all-zero filter checked == 0, NOT == 0xFF000000, so the all-black tile slipped through and got persisted to tileImages and disk cache. Brg-21 server-side guard: render loop now counts pixels with usable colour (color != null && color != NONE && col != 0). If count is zero, chunk is unrenderable in-memory and we sentinel instead of returning all-black tile. Panel's TileDiskCache keeps previously-rendered version of the chunk alive across the LOAD downgrade. Pnl-61 client-side filter: MapPane.onChunkTileResponse now also rejects mostly-black tiles (>=252 of 256 pixels at 0xFF000000). Pnl-61 disk cache cleanup: TileDiskCache.loadOne deletes any cached tile that's mostly-black on warm; user's existing black rectangles auto-clean next panel start. Versions: bridge 0.2.1 -> 0.2.2, panel 0.2.1 -> 0.2.2. Built and deployed; on next launcher boot mods/fathereye-bridge-0.2.1.jar auto-deletes and 0.2.2.jar takes its place via Bld-10 always-push-fresh launcher logic. -->
<!-- Brg-20 + Pnl-60 completed 2026-04-27: faster chunk fill, version 0.2.1. User: "chunk loading is now progressing but is going very slowly". Brg-20 per-render hoist: previous inner 256-pixel render loop made 256 expensive lookups per chunk - world.registryAccess().registryOrThrow(BIOME_REGISTRY) plus world.getBiome(pos) 8-corner BiomeManager noise blend per pixel. Hoisted both: registry handle resolved once per chunk, inner loop uses chunk.getBiomes().getNoiseBiome(x>>2, y>>2, z>>2) for direct 4x4x4 biome grid lookup (no blend). Fallback to world.getBiome(pos) if getBiomes() returns null (rare ProtoChunks before BIOMES status). Per-render cost drops ~1-2ms -> ~0.3-0.5ms. Brg-20 miss budget bump: MAX_MISSES_PER_TICK 16 -> 64. Combined with render speedup, worst-case tick cost ~64*0.5ms = 32ms, still inside 50ms budget. Throughput 320 chunks/sec -> 1280 chunks/sec. 3600-chunk world converges in ~3s vs ~11s. Pnl-60 panel-side fan-out: CHUNKS_TOPIC_FETCH_BUDGET 1000 -> 2500 to saturate the new bridge throughput at 1Hz snapshots. Triple audit (rule 9): clean. Noted that custom getMapColor on heavily modded servers (107 mods incl ProjectE/Mekanism/Thaumcraft) could exceed 0.5ms estimate; mitigation = existing watchdog catches sustained TPS dips. Versions bumped: bridge 0.2.0 -> 0.2.1, panel 0.2.0 -> 0.2.1. Built and deployed; existing mods/fathereye-bridge-0.2.0.jar will auto-delete on next launch and be replaced with fathereye-bridge-0.2.1.jar via Bld-10 always-push-fresh launcher logic. -->
<!-- Bld-10 completed 2026-04-27: launcher always pushes fresh bridge jar on boot + bridge/panel version bumps to 0.2.0. User: "still map panel is stopping loading chunks at 42%. console is being spammed by fathereye every second and i do not want this. i launched from the modpack test server folder and your fixes made no difference there" + "launcher has to always check and push a fresh bridge jar on boot" + "make sure you are updating version numbers for bridge so we can keep track easier". Two compounding bugs. (1) Build pipeline bug: bridge/build.gradle has `jar { enabled = false }` since the project ships a shaded fat jar produced by :shadowJar. Prior `./gradlew :bridge:reobfJar` invocations picked up the previously-built jar from build/libs/ and only rewrote names, never seeing freshly-compiled classes. Verified by extracting ChunkTileHandler.class from the deployed jar and grepping for "ChunkTile 60s:" - found old "ChunkTile 1s:". Build switched to `./gradlew :bridge:clean :bridge:shadowJar`; shadowJar.finalizedBy reobfShadowJar runs obfuscation on the fresh fat jar in one shot. (2) Launcher deploy bug: the C# launcher in panel/build.gradle only wrote the embedded bridge jar to <serverDir>/mods if NO fathereye-bridge-*.jar was present. Once any jar (even stale) existed, launcher refused to update, defeating the "ship new EXE to deploy new bridge" workflow. Fix: launcher ALWAYS overwrites with embedded jar on every launch, deletes any older-version fathereye-bridge-*.jar siblings so version bumps don't load two copies, and silently keeps existing jar if locked by running server JVM. Version bumps for traceability: bridge 0.1.0 -> 0.2.0, panel 0.1.0 -> 0.2.0. Updated: gradle.properties (bridgeVersion, panelVersion), Constants.BRIDGE_VERSION, App.VERSION, PlayerHeadCache User-Agent header. Built fresh bridge-0.2.0.jar (verified contents include "ChunkTile 60s:", "func_219219_b", "func_219287_e", BRIDGE_VERSION="0.2.0"). Repackaged single-file panel EXE (72 MiB), deployed to ModPack Server Test/. On next launcher boot the user's mods/fathereye-bridge-0.1.0.jar will be deleted and replaced with mods/fathereye-bridge-0.2.0.jar carrying Brg-17/18/19 + the audit fixes. -->
<!-- Brg-19 completed 2026-04-27: holder-level chunk fallback + log de-spam. User: "map panel is stopping loading chunks at 42%. console is being spammed by fathereye every second and i do not want this". Screenshot showed the Brg-18 1Hz diagnostic flooding the in-app console with hits=0 newFull=0 newPartial=0 sentinel(budget=N,null=16) every second, and the loading counter stalled at 42% (842/2025). Root cause of the 42% stall: even after Brg-18's FULL->HEIGHTMAPS->SURFACE fallback, chunks at ticket-level 44 (LOAD) still sentinel-rejected. In 1.16.5 ChunkSource.getChunk(cx, cz, status, false) checks the chunk's effective status from its ticket level via ChunkLevel.getStatus(level). A LOAD-level chunk maps to ChunkStatus.EMPTY, so even a SURFACE-status check fails even though the chunk's data is fully populated in memory. Fix: when all three status checks return null, fall through to the ChunkHolder directly via reflection. The holder's getLastAvailable() walks the per-status future list from FULL down and returns whatever IChunk is held, ignoring the level-to-status gating. SRG names verified against Forge userdev joined.tsrg at C:\Users\lukeo\.gradle\caches\forge_gradle\mcp_repo\de\oceanlabs\mcp\mcp_config\1.16.5-20210115.111550\joined\rename\config\joined.tsrg lines 12356-12420 (ChunkHolder block) and 12437+ (ChunkManager block): ChunkManager.func_219219_b(long) returns Lzr; (ChunkHolder = getVisibleChunkIfPresent, matches visible map WorldStateCollector iterates for chunks_topic); ChunkHolder.func_219287_e() returns Lcfw; (IChunk = getLastAvailable, cfw mapping verified at line 40258 of joined.tsrg). Both reflected via ObfuscationReflectionHelper.findMethod so dev (Mojang names) and prod (SRG names) both work. Log de-spam: the 1Hz diagnostic was at INFO level and panel ConsolePane shows everything, so user saw a line per second. Demoted to DEBUG and stretched cadence to 60s. Default logback root keeps DEBUG out of in-app console. Audit-flagged bug fixed: first cut of 60s rewrite called counter.getAndSet(0) unconditionally, then only logged if DEBUG. In production (root=INFO) this zeroed tallies every minute and silently discarded them, so any later flip to DEBUG would only show the post-reset window. Fix: bail out of maybeLogSummary on !isDebugEnabled() BEFORE touching counters. Audit fix #2: holder-fallback catch(Throwable t) logged t.toString() losing the stack trace; pass t as SLF4J last-arg so log4j prints full frame for diagnosing future SRG-name drift. Built and deployed bridge jar (6.17 MiB); next user start should fill the remaining ~1183 chunks instead of stalling at 42%. -->
<!-- Brg-18 + Pnl-57 + Pnl-58 + Pnl-59 completed 2026-04-27: map-hang fix + six audit-flagged bugs. User: "map loading is hanging" (screenshot showed loading counter stuck at 642/2025 chunks 32%); plus prior "everything has to be airtight, use many agents, overkill" surfaced six concrete bugs in the post-Brg-17 audit. Brg-18 root cause: WorldStateCollector.collectChunkPositions reports every ChunkHolder regardless of status; ChunkTileHandler only rendered ChunkStatus.FULL chunks. Chunks at LOAD/SPAWN/HEIGHTMAPS were in chunks_topic but bridge sentinel-rejected them forever, stalling the panel at the FULL subset. Fix: try FULL first, fall back to HEIGHTMAPS, then SURFACE. Don't cache fallback tiles so they get re-rendered once the chunk promotes. Brg-18 miss-budget bump 4 -> 16: cold-start fill rate jumps from 80 chunks/s to 320 chunks/s (~6s for 2025 chunks). Worst-case tick cost rises from 4-8ms to 16-32ms, still inside Forge's 50ms budget. Brg-18 NTP-jump fix: tick-budget reset switched from System.currentTimeMillis to System.nanoTime, immune to wall-clock backward jumps. Brg-18 diagnostic log: new "ChunkTile 1s:" summary emits per-second counters (cache hits, new full tiles, new partial/fallback tiles, sentinels by reason: budget vs chunk-null) so future map-stuck reports can be diagnosed from the server log. Pnl-57 volatile fields: MapPane.currentDim and expectedChunksThisDim now volatile; both cross threads (FX writes, IPC reader reads/writes) and the audit confirmed the race was real. Pnl-57 AtomicBoolean: requestTaskPending replaces volatile boolean check-then-set with AtomicBoolean.compareAndSet; FX thread and recursive debounce thread could both pass the gate spawning duplicate daemons. Pnl-57 PipeClient leak: App.tryConnect catch block now closes pipeClient on every connect/handshake failure path; previously the five throw sites in PipeClient.handshake leaked the socket FD until JVM exit. Pnl-58 BackupService partial-folder marking: backups copy into "world-<ts>.partial" then atomically rename via Files.move(ATOMIC_MOVE) to "world-<ts>" only on success. applyRetention filters endsWith(".partial"). New sweepStalePartials deletes any .partial older than 1h, called once at panel startup on FatherEye-PartialSweep daemon. Pnl-59 event_log retention: AppConfig.History.eventLogRetentionDays added (default 30d, 0 disables); MetricsDb.trimRetention adds DELETE FROM event_log WHERE ts_ms < cutoff in the existing 60-min sweep. Existing event_log_ts index keeps the predicate cheap. Triple audit (rule 9, 3 parallel agents): Brg-18 clean ship-it with concerns about tighter tick headroom on heavily loaded servers documented for monitoring the 1 Hz log. Pnl-57, Pnl-58, Pnl-59 all clean. One cosmetic FQN cleanup applied. Built bridge jar (6.17 MiB), repackaged panel EXE (72 MiB single-file), deployed both. -->
<!-- Pnl-56 + Brg-17 completed 2026-04-27: stop the "biomes must be length 256" console flood. User: "[image] server is running and i see map but now im getting constant looping error spamming console" + "i need everything working properly" + "everything has to be airtight, use many agents, overkill". Root cause: Brg-16 introduced length-0 sentinel arrays returned for "chunk not loaded" / "over miss budget" responses. The mapcore ChunkTile constructor enforces `Preconditions.checkArgument(biomes.length == 256, ...)`, so every sentinel triggered IllegalArgumentException at 43k+/sec, flooding the server console. Brg-17 fix: bridge builds a fresh 256-length zero sentinel ChunkTile per call via new `emptySentinel(dim, cx, cz)` helper. Length 256 satisfies the precondition. Allocating fresh per call (rather than reusing static arrays) keeps the sentinel immutable by construction so any future in-VM panel mode cannot contaminate bridge-side state. Pnl-56 fix: MapPane.onChunkTileResponse adds an all-zero detection block at the top -- if surfaceArgb is 256-length and every entry is 0, remove from `requested` and return BEFORE tileImages.put, tileData.put, or tileCache.save. Why all-zero is unambiguous: ChunkTileHandler line 141 builds `surfaceArgb[idx] = 0xFF000000 | rgb`, so a real loaded chunk's pixel is always at least 0xFF000000 (alpha set). MaterialColor.NONE fallback path uses 0xFF202020. A real chunk cannot produce all-zero ARGB, zero false positives. Defense in depth preserved: TileDiskCache.loadOne already deletes any all-zero tile encountered during cache warm (self-healing prior poisoned caches); ChunkSurfaceCache.put keeps `surfaceArgb.length != 256` rejection as redundant safety. Triple-audit (rule 9) dispatched 3 parallel agents on the diff. All three reported clean. Agent 1 flagged a latent static-array contamination hazard if the panel ever ran in-process; addressed by switching from shared static EMPTY_* arrays to fresh-alloc-per-call sentinel. Verification: rebuilt and deployed bridge jar; ran the Forge server for 19 minutes with the panel connected. Server log shows zero "biomes must be length 256" errors. Panel log shows clean handshake, tile cache warm of 642 tiles, and 1 Hz chunks_topic snapshots reporting 2025 loaded chunks end to end. Server shut down gracefully via TCP srv_stop RPC with all dimensions saving cleanly. Helper: stop-server.ps1 added at repo root for headless graceful shutdown via the bridge TCP transport (Hello + srv_stop). -->
<!-- Brg-16 completed 2026-04-27: server-side ChunkSurfaceCache to fix tick-thread starvation. User: "i started flying around and the chunks stopped loading in-game and they stopped loading on the map. approach for the map loading needs to have no impact on server performance and still load all loaded chunks on map". Diagnosis: ChunkTileHandler reads 256 block states + 256 biomes per chunk on the tick thread; with chunks_topic listing thousands of chunks per snapshot and the panel requesting tiles for all of them, ~1M tick-thread reads/sec starved Forge's chunk-gen task. New ChunkSurfaceCache (per-dim LRU, 16384 entries cap) fronts every chunk_tile RPC: cache hit returns in microseconds, cache miss computes once and caches forever. Per-tick miss budget MAX_MISSES_PER_TICK=4 caps tick-thread compute work at ~16% of a 50ms tick. Cache fills in ~12s for 1000 chunks; steady-state is 100% cache hits with negligible tick impact. Triple-audit reconciled (3 parallel agents flagged the SAME critical bug): empty-tile disk-cache poisoning. Bridge previously returned 256-zero arrays for "chunk not loaded" / "miss budget exhausted" responses; the panel persisted those to disk, blocking re-fetch forever. Fixes: bridge returns zero-LENGTH sentinel arrays (panel's existing length != 256 check then triggers failChunkRequest path which removes from `requested` without persisting); ChunkSurfaceCache.put rejects tiles with non-256-length surfaceArgb defensively; TileDiskCache.loadOne detects all-zero tiles and deletes them (cleanup of poisoned files from prior buggy versions); MAX_MISSES_PER_TICK lowered 8 -> 4 per audit recommendation. -->
<!-- Pnl-54 completed 2026-04-27: hourly world backups with 14-day age retention. User: "i want hourly world backups, they should be deleted when they are two weeks old". AppConfig.Backup adds hourlyBackups (default true), hourlyIntervalMs (1h), retainDays (14); retainCount default 10 -> 0 (unlimited within age window) per audit. App.wireLauncher arms a ScheduledExecutorService FatherEye-HourlyBackup at scheduleAtFixedRate(initialDelay=5min, period=1h) that calls BackupService.fromConfig(appConfig).runBackup() iff launcher.state()==RUNNING. BackupService applyRetention rewritten with combined age + count rules: delete if (mtimeMs < now-retainDays*24h) OR (rank >= retainCount, when retainCount>0). New backupTimestamp(Path) helper parses "world-yyyyMMdd-HHmmss-SSS" folder names (and legacy "world-yyyyMMdd-HHmmss") to derive timestamps. runBackup uses millisecond-precision folder names so two panels firing in the same second don't collide. Triple-audit reconciled (3 parallel agents flagged: SimpleDateFormat thread race, retention contradicting user intent, shutdown order, initial-delay coverage gap). Adopted: SimpleDateFormat -> DateTimeFormatter (immutable, thread-safe); retainCount=0 default (no count cap, age alone governs); App.stop graceful shutdown order (shutdown -> awaitTermination 15s -> shutdownNow); 5-min initial delay; ms-precision in folder name for multi-panel disambiguation. User's existing config.json patched: hourlyBackups=true, hourlyIntervalMs=3600000, retainDays=14, retainCount=0. -->
<!-- Pnl-53 completed 2026-04-27: persistent on-disk tile cache (vanilla-map-style "fill in as you walk") + cumulative loading progress + Fit-all button + larger chunks_topic budget. User: "i want the map to fill out like a vanilla map as they walk forward so that i can see a dynamic view of new chunks being rendered on the map" + "every chunk that has ever been loaded by any player to load and load dynamically as they explore". New TileDiskCache class persists every chunk_tile response to <serverDir>/fathereye-tiles/<dim>/<cx>_<cz>.tile (1048-byte little-endian binary, magic + version + cx + cz + ts + 256 surfaceArgb ints). MapPane.setTileCache attaches the cache after handshake (App.tryConnect uses marker.serverDir). warmCacheFromDisk runs on a background daemon thread; loaded tiles populate tileImages and a redraw fires. Each chunk_tile response also persists to disk async via TileDiskCache.save. Dim switch clears in-memory caches and re-warms from disk for the new dim. Cumulative loading progress: tileImages.size() / max(expectedChunksThisDim, loaded), no more flashing between snapshots. Fit-all button zooms to bbox of all loaded chunks. CHUNKS_TOPIC_FETCH_BUDGET 200 -> 1000. Diagnostic LOG.info on chunks_topic count changes for the user to verify the bridge is reporting. Triple-audit reconciled (3 parallel agents flagged the SAME critical bugs): (1) dim-switch race where in-flight chunk_tile responses for OLD dim corrupted NEW dim's tileImages keyed only by (cx,cz) -- fixed with dim-equality check at the top of onChunkTileResponse; (2) concurrent warm threads racing on tileImages -- fixed with AtomicInteger dimGeneration token captured at thread start, checked before each put; (3) TileDiskCache executor leak on setTileCache replacement -- fixed with new TileDiskCache.close() called on the previous instance; (4) silent canvas during warm -- fixed with "Loading cached tiles from disk..." status during warm. -->
<!-- Brg-15 completed 2026-04-27: ChunkManager.getChunks reflective lookup fixed for production runtime. Pnl-52's Brg-14 used `Class.getDeclaredMethod("getChunks")` which fails at runtime with NoSuchMethodException -- Forge 1.16.5 reobfuscates Mojang method names to SRG names at the .class file level, so at runtime the method's visible name is "func_223491_f", not "getChunks". User's server log confirmed: `[FatherEye-WorldState/]: ChunkManager.getChunks reflective lookup failed; chunks_topic coordinate list will be empty. java.lang.NoSuchMethodException: net.minecraft.world.server.ChunkManager.getChunks()`. Switched to net.minecraftforge.fml.common.ObfuscationReflectionHelper.findMethod with SRG name "func_223491_f" verified against joined.tsrg in Forge userdev cache (`f ()Ljava/lang/Iterable; func_223491_f` under class zs / net/minecraft/world/server/ChunkManager). Helper handles dev (Mojang) vs production (SRG) automatically. Pnl-52's all-loaded-chunks feature now actually works on the user's running server. -->
<!-- Pnl-52 + Brg-14 completed 2026-04-26: map shows all loaded chunks; offline-player persistence with greying + offline duration; online time in player info; chunk loading animation + percentage. User: "map is not updating correctly to show all loaded chunks, players are persisting on map when they log off, they need to be greyed out if they are not online with an indicator of how long they have been offline. also we need indicators on players in their info showing how long theyve been online. also we need a loading animation and percentage to illustrate the map loading chunks". (1) Bridge: ChunksSnapshot.DimChunks gains int[] chunks (flat coord array) capped at MAX_CHUNKS_REPORTED=4096; WorldStateCollector reflects ChunkManager.getChunks() (protected) via cached static Method handle; one-shot warn on reflection failure. (2) Panel: AUTO_SUBSCRIBE adds chunks_topic; dispatcher routes to MapPane.onChunksSnapshot which queues chunk_tile fetches up to CHUNKS_TOPIC_FETCH_BUDGET=200 per snapshot. (3) MapPane: Map<UUID, PlayerMarker> -> Map<UUID, PlayerEntry> with volatile online/lastSeenMs, 24h offline retention, render greys offline players (alpha 0.4 head + soft-grey name + "offline X" label below) + showPlayerInfoDialog shows ONLINE / OFFLINE plus "Online for X" (from PlayerMarker.onlineSinceEpochMs) or "Offline for Y" + "First seen Z ago". (4) Toolbar gets ProgressIndicator spinner + "Loading X / Y chunks (Z%)" label using monotonic-rising peakPending denominator. Triple-audit reconciled (3 parallel general-purpose agents): cached Method handle (was per-tick lookup); one-shot WARN on reflection failure; offline label moved cy+6 -> cy+18 to clear the head; PlayerEntry.online/lastSeenMs/marker marked volatile; ProgressIndicator + percentage in addition to the count to satisfy the user's "loading animation and percentage" request. -->
<!-- Pnl-51 completed 2026-04-26: orphan-server elimination via Win32 Job Object + dialog kill-and-restart. User: "we have to be certain there are never any orphans" + "i need it to wait to start the server until i click the save & start button". Diagnosis: an earlier panel session was killed via TaskKill /F (Pnl-18 noted this bypasses the JVM shutdown hook); the Forge server java.exe (PID 19704, parented by killed panel PID 21336) survived as an orphan. New panel auto-attached to the orphan, dialog showed "Server: RUNNING — Save & Start disabled". Two-prong fix: (1) WindowsJobObject helper (panel/src/main/java/io/fathereye/panel/launcher/WindowsJobObject.java) wraps Win32 CreateJobObject / SetInformationJobObject / AssignProcessToJobObject via JNA-platform with KILL_ON_JOB_CLOSE limit flag. ServerLauncher.start lazily creates the job and assigns the spawned JVM to it; when the panel JVM dies for any reason (graceful exit, TaskKill /F, BSOD, OS reboot) the kernel itself reaps the assigned process. Replaces the unreliable shutdown-hook-only guarantee. (2) PreBootDialog signature gets externalBridgeDetected param; serverIsRunning narrowed to mean panel-owned RUNNING (Save & Start truly disabled there). When external orphan detected (anyLiveBridge && !launcherRunning), Save & Start is ENABLED with label "Stop existing & Start"; click handler in App.openPreBootDialog calls killOrphanServers (ProcessHandle.destroyForcibly via marker PID, marker file deletion via m.markerPath) then sleeps 2s for OS to release session.lock then launcher.start. Triple-audit reconciled (3 parallel general-purpose agents). CRITICAL fix adopted: removed the autoStart-time killOrphanServers call -- all 3 audits independently flagged that it would kill panel A's healthy server when panel B started up (markers carry only server PID with no panel-owner identity, so multi-panel hostility was unavoidable). Job Object handles all FUTURE orphans; pre-existing orphans must be handled via the user's explicit Stop existing & Start click in the dialog (intentional consent only). Other audit fixes: m.markerPath used instead of recomputing the bridges-dir path; Job Object create/assign failures now also push a WARNING through stdoutSink so the user sees them in the in-app console (not just panel.log). -->
<!-- Pnl-50 completed 2026-04-26: companion outputs in Evaluations/. EvaluationGenerator.generate() now writes 3 additional artifacts alongside the markdown report on every server stop/crash: console-<ts>.log (every console_log line in chronological order with full ISO 8601 timestamps), metrics-<ts>.csv (per-second tps20s/tps1m/tps5m/msptAvg/msptP95/msptP99/heap/heapPct/gcMs/cpu/threads), alerts-<ts>.csv (one row per alert with severity/kind/msg). Each companion is in its own try/catch so partial failure doesn't block the markdown write. Triple-audit fixes adopted: ISO_TS (yyyy-MM-dd'T'HH:mm:ss.SSSXXX) used for all CSV / console-archive timestamps instead of HUMAN_TS with locale-dependent timezone abbreviation that broke Excel parsing; CSV csvField() also escapes carriage return so Windows-CRLF embedded in alert messages doesn't corrupt row boundaries. -->
<!-- Pnl-49 completed 2026-04-26: verbose logging end-to-end + dialog always shows. Two related fixes: (1) bridge IpcAppender's Log4j2 attach level changed Level.INFO -> Level.ALL. The user's running server's debug.log was 2.4 MiB / 30 min on a 100+ mod modpack, full of DEBUG events that the bridge was dropping at our appender's INFO threshold before they reached the panel. With Level.ALL the bridge forwards whatever the root logger admits, so on a Forge default config the panel sees the same INFO traffic as before, and on a manually-DEBUG-configured root, all DEBUG events flow through to the panel console live. Backpressure handled upstream in Publisher's MpscArrayQueue / panel ConsolePane 600-line ring. (2) App.startInternal autoStart no longer skips the pre-boot dialog when an external bridge is already running; instead the dialog always shows and the dialog detects the live bridge via anyLiveBridge() (line 730). Save & Start is disabled when serverRunning = launcherRunning || externalBridge so we cannot collide on world/session.lock. The user reported "now the memory pop up isnt coming up when i open it" because anyLiveBridge() short-circuited the dialog whenever they had a server running from a prior session. Triple-audit reconciled (3 parallel general-purpose agents): ISO 8601 timestamps in CSV / console (was HUMAN_TS with locale TZ); csvField escapes CR; AppConfig.showPreBootDialogOnStart default initializer = true verified. Required user action documented: stop and restart the running Forge server JVM to load the new bridge bytecode. -->
<!-- Pnl-48 completed 2026-04-26: verbose logging. logback.xml root level bumped INFO -> DEBUG so panel.log captures every IPC/launcher/watchdog/PipeReader event during boot. Per-logger overrides for FatherEye-* keep all panel components at DEBUG; third-party noise (org.sqlite, com.sun.jna, javafx, com.fasterxml) capped at INFO so the log stays focused. Synthetic in-app console lines added on bridge-discovery start ("Bridge marker discovery started; will poll for handshake.") and on handshake success (full version banner with mc/forge/mod count/dim count). 5 MiB rolling file appender keeps disk usage bounded even at DEBUG. -->
<!-- Pnl-47 completed 2026-04-26: deep evaluation reports. EvaluationGenerator rewritten end-to-end. New ExtStats class retains all values to compute interpolated percentiles (p10/p25/p50/p75/p90/p95/p99 across the session). New sections: dedicated Heap & Memory (configured -Xmx, used/% distribution, pressure windows >=75/85/90/95%, peak time, sparkline), Garbage Collection (total GC time, seconds with GC, overhead %, max single-second pause with timestamp, full distribution table), CPU & Threads (distribution table, time at >70/85/95%, thread drift detection, CPU sparkline), Lag-Spike Analysis (auto-detected runs of TPS<18 with start/end/duration/min-TPS/max-MSPT, ranked by severity), Mod Impact by Dimension (per-dim breakdown with top-15 mods per dim), Player Sessions (reconstructed first/last seen + total online time per player), Mobs trend (avg/peak total + peak per dimension), Alerts Timeline (full list, no cap), Console Highlights (top WARN/ERROR sources), Crash Analysis (last 50 console lines + Exception/Caused-by extracts, only on non-zero exit). ASCII sparkline helper using Unicode block chars. Existing sections retained and extended with percentile distribution table. -->
<!-- Pnl-46 completed 2026-04-26: two small UX bugs. (1) Save & Start dialog button rendered as "Save && Start" because JavaFX ButtonType labels do NOT use Win32/Swing-style "&&" escaping; the label is rendered verbatim. Changed to single ampersand. (2) Console pane appeared empty unless user manually clicked Auto-scroll. The Pnl-40 tail-f auto-pause listener was the cause: on the first render before the TextArea has been laid out (e.g. when Console tab is not the active tab), setScrollTop(Double.MAX_VALUE) was stored verbatim and lastKnownMaxScroll captured MAX_VALUE; on the next layout pass the actual scrollTop clamped to a real value (~100), the listener computed (100 >= MAX_VALUE - 8) == false, and silently auto-unchecked Auto-scroll. Removed the auto-pause-on-scroll listener entirely; user controls pause via the explicit checkbox. renderAndSnap also now does setScrollTop(MAX_VALUE) twice -- once now, once via Platform.runLater on the next pulse -- so the bottom is reliably hit even if the first call landed before layout. Dead fields lastKnownMaxScroll + ignoreScrollChange removed. -->
<!-- Pnl-45 completed 2026-04-26: CRITICAL deadlock fix in tryConnect + state-aware polling status. Pnl-43 introduced the deadlock by gating autoStart behind the pre-boot dialog: openPreBootDialog submits launcher.start to ipcExecutor, but App.startInternal had also submitted tryConnect to the same single-threaded ipcExecutor BEFORE the dialog appeared. tryConnect's poll-and-Thread.sleep(2000) loop monopolised the IPC thread for the full bridgeMarkerTimeoutMs (Pnl-44 made that 600s = 10 min), so launcher.start sat queued behind it and the JVM never spawned. The user saw "230s elapsed" with no server actually running. Fix: tryConnect runs on its own dedicated FatherEye-Connect daemon thread (not ipcExecutor); same fix for the onBridgeDisconnect reconnect watcher (FatherEye-Reconnect). Bonus state-aware polling: when launcher state is STOPPED/CRASHED, the loop shows "No server running. Use Configure or Start" instead of pretending to count elapsed time, and the deadline only ticks during actual STARTING/RUNNING attempts. -->
<!-- Pnl-44 completed 2026-04-26: state-machine + bridge marker timeout fix for heavy modpacks. Three issues: (1) Toolbar said "Server: RUNNING" while Forge was still mid-mod-load. ServerLauncher.start() flipped state to RUNNING immediately after process.start(), but on a 100+ mod modpack the JVM takes 5-10 minutes to reach FMLServerStartedEvent. Removed the immediate transition; new markRunning() method promotes STARTING -> RUNNING and is called from App.tryConnect after bridge handshake (the real "ready to serve" signal). (2) Bridge marker poll deadline was hardcoded 90s; on heavy modpacks the bridge isn't even REGISTERED yet at 90s, so panel timed out and stats/players never populated. New AppConfig.serverRuntime.bridgeMarkerTimeoutMs (default 600_000ms = 10 minutes); user's existing config.json hand-patched. (3) Console blank during silent classloader phase. State-sink now emits synthetic console lines on STARTING ("Server JVM spawned. Waiting for Forge classloader...") and RUNNING ("Bridge handshake complete..."). Status bar wait message now shows elapsed/budget seconds. Audit fix adopted: seed watchdog.heartbeat() right after markRunning() so the watchdog's no-heartbeat WARN doesn't false-trip on long boots (5+ min). -->
<!-- Pnl-43 completed 2026-04-26: pre-boot dialog auto-shows on autoStart. Pnl-42 added the dialog as a manual Configure button only; the user reported "i didnt get a config pop up for server ram usage" because autoStart fires immediately on panel launch and the Configure button auto-disables once state leaves STOPPED/CRASHED, so the user never gets a chance to click it. New AppConfig.serverRuntime.showPreBootDialogOnStart (default true). App.startInternal autoStart block now branches: anyLiveBridge -> attach mode; showPreBootDialogOnStart -> Platform.runLater(this::openPreBootDialog) instead of unconditional launcher.start (the user's Save & Start triggers boot, Save/Cancel leaves server stopped); else -> direct launcher.start as before. PreBootDialog gets a "Show this dialog automatically before every server start" CheckBox seeded from current config; Result.showOnNextStart carries the choice back; App.openPreBootDialog persists into AppConfig. Audit fix adopted: re-check anyLiveBridge() at click time inside ipcExecutor before launcher.start() so a second panel or external server start during the dialog doesn't collide on world/session.lock. -->
<!-- Pnl-42 completed 2026-04-26: pre-boot configuration modal. New "Configure..." toolbar button (between title and Start) opens a modal with two tabs. JVM & Memory tab: dedicated Xmx/Xms TextFields + checkboxes for UseG1GC / ParallelRefProcEnabled / HeapDumpOnOutOfMemoryError / -noverify + a MaxGCPauseMillis field + a free-form "Other JVM args" TextArea, all parsed out of the existing AppConfig.serverRuntime.jvmArgs string. server.properties tab: editable TableView seeded with 18 common keys plus all keys actually present in the file, edits committed in place. Cancel / Save / Save & Start buttons. Save & Start is disabled while server is RUNNING. New ServerProperties helper preserves comments and ordering on disk. ServerLauncher.spec field changed from final to volatile, with a new updateSpec() method that throws IllegalStateException unless STOPPED/CRASHED. App.openPreBootDialog wires the button: applies jvmArgs to AppConfig + persists JSON, writes server.properties, calls launcher.updateSpec, optionally launcher.start(). Triple-audit reconciled: edit-commit gap on Save (force propsTable.edit(-1, null) via ActionEvent filter) + Xmx/Xms/gcPause format validation + tmpdir-fallback removal (visible warning Label instead) + Configure button gated to STOPPED/CRASHED via launcher state-sink + EvaluationGenerator lambda reads launcher.spec().workingDir live (not stale wireLauncher-time runSpec capture) + immediate "Launching..." status update before launcher.start. -->
<!-- Pnl-41 completed 2026-04-26: alert UX overhaul + UTF-8 encoding root cause. Root cause of the "Father Eye â€" Low TPS" mojibake was javac falling back to Windows-1252 source encoding when compileJava had no options.encoding set, so UTF-8 source bytes E2 80 94 (em-dash) compiled as three Windows-1252 chars. Fixed at the source: root build.gradle now applies options.encoding='UTF-8' to every subproject's JavaCompile via allprojects { tasks.withType(JavaCompile).configureEach }. Modal Alert dialog replaced with MainWindow.showAlertBanner: non-modal in-window banner StackPane-overlaid on the tab area, warning-amber #7a5510/#fff2c8 palette, 12s auto-dismiss via PauseTransition, click-to-dismiss. Player-join suppression finally wired: AlertEngine.onPlayersSnapshot detects count increase and disarmFor(30_000), with tpsBelowSinceMs reset so pre-join accumulation doesn't survive. Default tpsDropThreshold 18.0->16.0 and tpsDropSustainSec 10->30 for fresh installs; user's existing config.json hand-patched to the same. Triple-audit reconciled: encoding fix adopted as root-cause; 12s/amber/StackPane overlay adopted; idempotent bindMainWindow + alerts-counter declined. -->
<!-- Pnl-40 completed 2026-04-26: Console + Config promoted from bottom-split panes to top-level Tabs in the single TabPane; the bottom SplitPane is gone so the log fills the full window when its tab is active. ConsolePane gains an Auto-scroll CheckBox + Dropped counter toolbar; renderAndSnap uses setScrollTop(Double.MAX_VALUE) (replaces unreliable positionCaret) and is suppressed while CheckBox is OFF (ring still drains so bridge IPC doesn't cork). Tail-f auto-pause: scrollTopProperty listener auto-unchecks the CheckBox on user wheel/scrollbar drag above the bottom and auto-re-checks on scroll-to-bottom. ignoreScrollChange flag suppresses programmatic scroll resets. Triple-audit reconciled (A3 must-have wheel auto-pause adopted; A2 nice-to-have pre-layout snap declined as niche; A2 ConfigPane scope creep raised to user). -->
<!-- Pnl-39 completed 2026-04-26: Mods Impact tab shows all 100+ installed mods, not just the 5-10 with active impact. WelcomeInfo gains String[] modIds (built from WelcomePayload.mods which the bridge already populates from ModList.get().getMods()); App.tryConnect calls ModsPane.setKnownMods after handshake; ModsPane caches lastPayload + knownMods, re-renders on either input changing, merges synthesized "(all dims, Idle)" rows for any installed mod missing from the impact snapshot. Header label updates per render with "N active, M idle". setKnownMods clears lastPayload to avoid stale impact rows surviving across reconnects. Sort: impact desc, then mod-id alpha (stable). Triple-audit reconciled (A1 reconnect-clear, A3 dim label + alpha sort + header count). -->
<!-- Pnl-38 completed 2026-04-26: ConsolePane batched flush fix. onLogLine enqueues to ConcurrentLinkedQueue; 10 Hz AnimationTimer drains <=200 lines/tick into a 600-line ArrayDeque ring and rewrites the TextArea via single setText. Eliminates the FX-thread freeze caused by TextArea.appendText O(n) per call on heavy mod-load WARN streams. -->
<!-- Pnl-36 + Pnl-37 completed 2026-04-26: EvaluationGenerator writes <serverDir>/Evaluations/evaluation-<ts>.md with TPS/MSPT/heap/GC/CPU/threads stats + per-mod ranked tick impact + alerts/console-error tally + recommendations on every server STOPPED/CRASHED transition; PlayerHeadCache switched from Crafatar (UUID-only, offline-UUID failure) to mc-heads.net (name-based, online or offline mode). -->
<!-- Pnl-35 + Bld-09 completed 2026-04-26: replaced IExpress wrapper with self-contained C# launcher (embeds bundle + bridge jar as PE resources, finds server folder via MainModule.FileName) and wired AppConfig into ServerLaunchSpec via new fromConfig(AppConfig) so the cwd auto-detection actually drives the launched JVM's workingDir. Fixed stale-marker reconnect by using anyLiveBridge() with PID validation. -->
<!-- Pnl-34 completed 2026-04-26: AppConfig.load() auto-detects when cwd looks like a Forge 1.16.5 server (mods/ + eula.txt or similar) and overrides serverRuntime.workingDir + backup.backupDir to cwd. Same EXE works in multiple server folders without per-folder config. -->
<!-- Pnl-33 completed 2026-04-26: disconnect/reconnect plumbing — PipeReader on-disconnect hook closes pipeClient + disarms watchdog + posts UI status + schedules 5-min auto-reconnect; ServerLauncher.stopFast() with 7s grace for the shutdown hook so Windows' ~10s JVM-shutdown allowance doesn't TerminateProcess us mid-stop and orphan the server; forceKillTree kills descendants too. -->
<!-- Pnl-32 completed 2026-04-26: PlayerHeadCache async-fetches real player skins from Crafatar (size=20, overlay=on) and the map renderer uses them in place of the stylised Steve placeholder. java.net.http added to jpackage --add-modules so HttpClient is in the bundled JLink runtime. -->
<!-- Pnl-30 + Pnl-31 completed 2026-04-26: top-mod load factor in Server Health Assessment (StatsPane) + per-mod Impact column with descriptor + inline progress bar in ModsPane. -->
<!-- Pnl-29 completed 2026-04-26: Server Health Assessment on Stats — composite 0–20 score with descriptor (Excellent/Good/Fair/Poor/Critical) and per-load-factor ProgressBars (TPS deficit, MSPT load, heap pressure, GC pressure, CPU load) so the operator sees at a glance what is consuming server budget. -->
<!-- Pnl-27 + Pnl-28 completed 2026-04-26: StatsPane adds GC/threads/CPU charts; Watchdog deferred items finished — nanoTime clock, toolbar heartbeat badge, broader heartbeat signal via PipeReader frame-hook (any inbound frame counts, not just tps_topic). -->
<!-- Bld-08 + Bld-09 + Pnl-26 completed 2026-04-26: true single-file Windows .exe via IExpress + native C# launcher + auto-deploy of bridge jar to host server's mods folder; .dmg Mac build wired through new GitHub Actions workflow (macos-latest runner) since jpackage can't cross-compile from Windows; pre-stop backup skips session.lock + locked files instead of aborting. -->
<!-- Pnl-24 + Pnl-25 + Bld-07 completed 2026-04-26: full MapPane rewrite (cached Image tiles, spatial culling, drawImage per chunk, debounced RPCs, bigger stylised Steve heads, bold names, click-to-info, right-click admin context menu); PipeClient fail-fast on closed pipe (no more socket-error spam after server crash); :panel:distributableZip Gradle task building a self-contained 189MB wrapper for distribution. -->
<!-- Brg-13 + Pnl-23 completed 2026-04-26: server crashed via ChunkTileHandler forcing synchronous chunk generation on the tick thread (60s tick-watchdog tripped on a panel zoom-out enqueueing 64 RPCs). Fix: bridge uses ChunkSource.getChunk(load=false) → returns empty tile if not loaded; panel lowers per-frame chunk_tile budget 64 → 16. -->
<!-- Pnl-22 + Brg-12 completed 2026-04-26: switched Windows transport to TCP localhost. Bypasses the named-pipe deadlock entirely. All 5 topic subscriptions complete in <1 ms after handshake; map renders chunks; panel reliably runs 5+ minutes. User confirmed: "map is working". -->
<!-- Pnl-20 + Pnl-21 + Brg-11 completed 2026-04-26: OVERLAPPED I/O on both pipe ends to resolve the kernel-level read/write deadlock. Synchronous named-pipe handles on Windows serialise concurrent read+write inside the kernel; this was the actual root cause of every "panel hung after handshake" since v0.1.0. Both panel and bridge now open with FILE_FLAG_OVERLAPPED and route I/O through OVERLAPPED structs + per-direction events + GetOverlappedResult. JNA 5.14's Kernel32 omits GetOverlappedResult; declared a Kernel32Ex extension in each module. -->
<!-- Pnl-19 completed 2026-04-26: removed FlushFileBuffers from WindowsPipeTransport.writeAll. THIS was the original root cause of every "panel hung after handshake" report. FlushFileBuffers on a named-pipe handle blocks until the peer reads, wedging the panel mid-subscribe-loop. Subsequent topics (console_log, players_topic, mobs_topic, mods_impact_topic) were never subscribed, hence the blank map. Watchdog cascade (Pnl-16) was masking this; once the cascade was fixed the underlying wedge surfaced. -->
<!-- Pnl-18 completed 2026-04-26: orphaned-server defense — Runtime.addShutdownHook in App.startInternal calls launcher.stop() so a Task Manager "End task" on the panel no longer leaves the MC server child running headless and holding world/session.lock. Hard TerminateProcess / End-process-tree still bypasses (needs Win32 Job Object, deferred). -->
<!-- Bld-06 completed 2026-04-26: :panel:deployToServer Gradle task flattens jpackage output into the server folder root (EXE beside mods/world/start.bat, app/ + runtime/ as siblings). Replaces nested-subfolder layout per user directive. -->
<!-- (Pnl-16 / Pnl-17 entries continue below) -->
<!-- Pnl-16 completed 2026-04-26: watchdog cascade fix + auto-restart disabled by default. arm() resets lastHeartbeatMs, tick() measures from max(lastHeartbeatMs, armedAfterMs), defaults bumped to 90 s/240 s, watchdogAutoRestart config flag (default false), session.lock retry polling (15 s budget), Start/Restart buttons moved off FX thread, narrowed canAcquireLock catch, disarm-on-failure on all start/restart paths. Triple-audit reconciled (A#2/A#5/A#6/A#9, B#A, C#1/C#3). Deferred: clock-skew migration to nanoTime, watchdog UI surface, broader heartbeat signal — all raised to user per Standing Rule 11. -->
<!-- Pnl-17 completed 2026-04-26: MapPane.bindPipeClient triggers Platform.runLater(this::redraw) so the chunk_tile fan-out fires on fresh handshake even when no players are online and the user hasn't interacted. Previously the canvas only redrew on size change, mouse events, players snapshot, or chunk response — none of which fire when handshake completes mid-server-life with zero online players, so the map stayed blank until the user moved the mouse. -->
- `Bld-NN` — root build / Gradle / wrapper / packaging
- `Mc-NN`  — mapcore library
- `Brg-NN` — bridge Forge mod
- `Pnl-NN` — panel JavaFX desktop app
- `Map-NN` — admin map Forge mod (owned by parallel session)
- `Doc-NN` — docs and protocol contracts

## Active Claims
| Agent | Developer | Files Claimed | Purpose | Claimed At |
|-------|-----------|---------------|---------|------------|
| Map-* | Other Claude session | map/** (entire subtree) | In-game admin map mod, owned by parallel session | 2026-04-25 |

<!-- Bld-01 completed 2026-04-25: initial scaffolding (settings.gradle, build.gradle, gradle.properties, wrapper, .gitignore, README, CHANGELOG, CLAIMED). -->
<!-- Bld-02 completed 2026-04-25: pinned Gradle daemon to JDK 17 via org.gradle.java.home; restored JavaFX plugin in panel. -->
<!-- Bld-03 completed 2026-04-25: panel:jpackageExe task added (installDist + jpackage app-image with module-path JavaFX). -->
<!-- Mc-01 completed 2026-04-25: mapcore 0.1.0 — interfaces (MapTexture, MapTextureLoader, MapGraphics, MapRenderer), POJOs (MapData, MapViewState, ChunkTile, EntityMarker, PlayerMarker, PointOfInterest, LayerToggleSet), NoOpMapRenderer, JUnit tests, published to local-maven. -->
<!-- Mc-02 completed 2026-04-25: mapcore 0.2.0 — BasicMapRenderer (chunk pixels + entity/player/POI markers + layer gating), MapCore.basicRenderer() factory, contract doc bumped to 0.2.0. -->
<!-- Brg-01 completed 2026-04-25: bridge pipe + handshake — FatherEyeBridge @Mod, BridgeLifecycle, NamedPipeServer (JNA), Frame/Codecs/IpcEnvelope/MarkerFile, IpcSession with Hello/Welcome, PipeAcceptLoop. -->
<!-- Brg-02 completed 2026-04-25: TPS/JMX sampler — TpsCollector (rolling 1200-tick MSPT percentiles + 20s/1m/5m TPS buckets), JmxSampler (heap, GC delta, OS process CPU, thread CPU), Subscriptions, Publisher with 1Hz scheduled tps_topic. -->
<!-- Brg-03 completed 2026-04-25: log appender — IpcAppender (programmatic Log4j2 attach to root), console_log topic. -->
<!-- Brg-04 completed 2026-04-25: world state collector — players_topic, mobs_topic, chunks_topic via WorldStateCollector enumerating server.getPlayerList() + ServerWorld.getAllEntities() + getChunkSource().getLoadedChunksCount(). -->
<!-- Brg-05 completed 2026-04-25: per-mod profiler (proportional approximation) — WorldTickProfiler accumulates per-dim wall time, ModsImpactCollector attributes nanos proportionally to entity+TE counts per mod namespace. -->
<!-- Brg-06 completed 2026-04-25: RPC dispatch — RpcDispatcher with server.execute pattern, RpcHandlers (cmd_run/tp/kick/ban/op/whitelist/killEntity/clearMobsInArea/weather/time/srv_stop/srv_restart). -->
<!-- Brg-07 completed 2026-04-25: chunk_tile RPC — ChunkTileHandler returns mapcore ChunkTile (heightmap + surface ARGB via getMapColor + biome id). -->
<!-- Brg-08 completed 2026-04-25: JFR controller — JfrController loaded via reflection so JDK 8 servers don't crash on class load. registerJfr in RpcHandlers. -->
<!-- Pnl-01 completed 2026-04-25: panel skeleton — Launcher + App + MainWindow, PipeClient (CreateFile + WaitNamedPipe), MarkerDiscovery, PipeFrame/PipeCodecs/PipeEnvelope. -->
<!-- Pnl-02 completed 2026-04-25: StatsPane — JFreeChart-FX charts (TPS/MSPT/heap), live readout labels, PipeReader + TopicDispatcher, auto-subscribe to tps_topic. -->
<!-- Pnl-03 completed 2026-04-25: ConsolePane — read-only TextArea + command bar wired to launcher.sendCommand fallback. -->
<!-- Pnl-04 completed 2026-04-25: ServerLauncher — ProcessBuilder + stdout pump + exit watcher, Watchdog with TPS heartbeat, toolbar Start/Stop/Restart buttons. -->
<!-- Pnl-05 completed 2026-04-25: PlayersPane + MobsPane — TableViews from players_topic / mobs_topic, count/grand-total labels. -->
<!-- Pnl-06 completed 2026-04-25: ModsPane — TableView from mods_impact_topic, sorted by attributedNanos descending, with approximation note. -->
<!-- Pnl-07 completed 2026-04-25: console-bar wired to bridge cmd_run via sendRequest, fallback to launcher stdin if pipe closed. -->
<!-- Pnl-08 completed 2026-04-25: JfxMapGraphics + MapPane — Canvas-backed map view with pan/zoom/dim picker, on-demand chunk_tile fetching, PlayerMarker overlay. Map tab is default. -->
<!-- Pnl-09 completed 2026-04-25: MetricsDb — SQLite WAL with metric_raw/1m/1h + event_log, daemon writer thread, scheduler-driven downsample/trim. -->
<!-- Pnl-10 completed 2026-04-25: AppConfig + AlertEngine + ConfigPane — JSON config persisted to %LOCALAPPDATA%/FatherEye/config.json, threshold-based alerts (TPS/heap/MSPT), JSON editor with Save/Reload. -->
<!-- Pnl-11 completed 2026-04-25: BackupService + RestartScheduler — pre-stop world copy with retention, daily restart at 04:00 local. -->
<!-- Pnl-12 completed 2026-04-25: protocol-mismatch warning on Welcome (full modal dialog deferred to next major bump). -->
<!-- Map-01 completed 2026-04-25: fathereye-map skeleton — mods.toml with optional fathereye_bridge dep, pack.mcmeta, FatherEyeMap @Mod that links against mapcore. Body owned by parallel session per HANDOFF.md. -->
<!-- Audit-fix-01 completed 2026-04-25: triple-audit reconciliation — JFR reflective load, biome registry lookup, MixinConfigs manifest removed, PipeClient lock split + pendingResponses drain on close, MetricsDb single-thread DB ops, MapPane ConcurrentHashMap + LRU + per-frame request budget, App.stop ordering fix, Welcome dimensions surfaced, docs updated. -->
<!-- Bld-04 + Pnl-13 completed 2026-04-25: server-side-only deployment — shadowJar fat bridge, autoStart, "Server Start with Father Eye" exe, mods.toml side=SERVER + displayTest=IGNORE_SERVER_VERSION (commit fb5f2b5). -->
<!-- Pnl-14 completed 2026-04-25: panel instant-close fix — explicit JDK module list to jlink, logback file appender, defensive App.start with uncaught handler, auto-start moved off FX thread (commit ffc7978). -->
<!-- Brg-09 completed 2026-04-25: bridge multi-release exclusion in shadowJar so SpecialSource reobf finishes (commit 9f6d50e). -->
<!-- Pnl-15 + Brg-10 completed 2026-04-25: TransportServer / Transport abstractions, TcpTransportServer + TcpTransport (cross-platform), BridgeLifecycle OS-aware factory, jpackageMacApp task (commit da83429). -->
<!-- Audit-fix-02 completed 2026-04-25: second triple-audit pass — TCP server-socket lifecycle owned externally (no per-cycle rebind race), disconnect doesn't null fields, shadowJar.finalizedBy reobfShadowJar, ServerLaunchSpec OS-aware defaults, marker pipeName only for named-pipe, anyLiveBridge PID-recycling guard, README + ipc-protocol marker schema. -->

## Permanently Shared Files (edit carefully, pull first)
These files are touched by many systems — extra caution when editing:
- `settings.gradle` — root project list
- `gradle.properties` — shared version constants (forge, mc, javafx, mapcoreVersion) AND the JDK 17 daemon pin
- `build.gradle` (root) — group/version constants
- `mapcore/src/main/java/io/fathereye/mapcore/api/**` — public API; any change here forces a mapcore version bump and breaks the parallel session's `map/` build
- `docs/mapcore-api-contract.md` — versioned contract; only edit when bumping mapcore version
- `docs/ipc-protocol.md` — bridge ↔ panel wire format; both sides must move together
- `bridge/src/main/java/io/fathereye/bridge/topic/Topics.java` — topic name constants
- `bridge/src/main/java/io/fathereye/bridge/util/Constants.java` — pipe prefix, bridge version, protocol version
- `panel/src/main/java/io/fathereye/panel/App.java` — wires every subsystem; high coupling
